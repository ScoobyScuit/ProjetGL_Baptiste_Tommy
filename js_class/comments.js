class Comments {
  constructor(id, idTask, idUser, contenu, date) {
    this.id = id;
    this.idTask = idTask;
    this.idUser = idUser;
    this.contenu = contenu;
    this.date = date;
  }

  addComment(idUser, idTask, comment) {
    // TODO
  }

  modifyComment(idComment, newComment) {
    // TODO
  }

  deleteComment(idComment) {
    // TODO
  }
}
